# Demo Assets

- `sample-rules.txt`: starter global rules.
- `sample-document-notes.txt`: plain text that you can copy into a DOCX file for testing.

To create a DOCX for demo:
1. Open a word processor.
2. Paste the contents of `sample-document-notes.txt`.
3. Save as `sample-document.docx`.
